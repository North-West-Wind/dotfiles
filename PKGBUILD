# Maintainer: NorthWestWind <wsyn148@gmail.com>
pkgname=hyprsquid-meta
pkgver=1.0.0
pkgrel=1
pkgdesc="Meta-package for HyprSquid dependencies"
arch=("any")
url="https://github.com/North-West-Wind/dotfiles"
license=('GPL3')
depends=(
	# Core Hyprland
	"hyprland"
	# Hyprland utils
	"hyprcursor"
	"hypridle"
	"hyprlock"
	"hyprpaper"
	"hyprpolkitagent"
	"hyprshot"
	"hyprswitch"
	"wl-clipboard"
	# Screen sharing (for OBS)
	"xdg-desktop-portal-hyprland"
	# Launchers and bars
	"rofi"
	"walker-bin"
	"waybar"
	# Apps
	"dolphin"
	"konsole"
)
optdepends=(
	# Some tray stuff
	"blueman"
	"fcitx5"
	"network-manager-applet"
)
source=()
sha256sums=()

prepare() {
	echo "Pretend you're preparing..."
}

build() {
	echo "Pretend you're building..."
}

package() {
	echo "Pretend you're packaging..."
}
